[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "hr-bridge-pico"
dynamic = ["version"]
description = "Relay heart rate from a Raspberry Pi Pico W over USB serial to hr-osc"
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
authors = [{ name = "RealWhyKnot" }]
keywords = ["heart rate", "bluetooth", "ble", "vrchat", "osc", "hr-osc"]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia",
]
dependencies = ["pyserial>=3.5"]

[project.urls]
Homepage = "https://github.com/RealWhyKnot/hr-bridge-pico"
Issues = "https://github.com/RealWhyKnot/hr-bridge-pico/issues"

[project.scripts]
hr-bridge-pico = "hr_bridge_pico.__main__:main"

[tool.setuptools.dynamic]
version = { attr = "hr_bridge_pico.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 120
exclude = [".git", ".ruff_cache", ".venv", "build", "dist"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
